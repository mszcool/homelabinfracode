# Values to exclude from docs secrets scanning.
# One value per line. Lines starting with # are comments.
#
# These are generic system usernames, well-known public values, or
# service/software names that configs.private/ happens to contain
# but are not actual secrets.

# Generic system usernames
root
Administrator
provided

# Well-known public DNS servers
8.8.8.8
8.8.4.4

# Version-like strings that happen to parse as IPs
25.04.2.5

# Service/software names used as VM hostnames — not secrets.
# These match real playbook/script filenames (identity-samba4-addc-setup.yaml,
# manage-truenas-scale.sh) which must remain as-is in docs.
samba4-addc
truenas-scale
