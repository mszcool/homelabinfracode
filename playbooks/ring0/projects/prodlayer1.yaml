---
# Production Layer 1 Project Configuration for Incus
# This file defines the preseed configuration for the prodlayer1 project
name: prodlayer1
config:
  features.images: true
  features.networks: false
  features.profiles: true
  features.storage.volumes: true
  limits.disk: "{{ tp_incus_config.project_disk_limits.production }}"
  restricted: true
  restricted.networks.access: phys-br,iso-nat
  restricted.devices.gpu: allow
  restricted.devices.usb: allow
description: Production VMs (prodlayer1) with bridged networking and higher storage limits