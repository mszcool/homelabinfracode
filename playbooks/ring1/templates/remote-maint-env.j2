# Managed by Ansible — do not edit manually
# Sourced by ~/.profile (interactive SSH) and crontab entries.
export depthIp="{{ remote_maint_depth_ip }}"
export depthSecret="{{ remote_maint_depth_secret }}"
export mqttServer="{{ remote_maint_mqtt_server }}"
export mqttUser="{{ remote_maint_mqtt_user }}"
export mqttPwd="{{ remote_maint_mqtt_pwd }}"
